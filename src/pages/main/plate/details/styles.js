import styled from "styled-components/native"
import { Colors } from "../../../../styles"

export const ContainerPlateTransactions = styled.View`
  background: ${Colors["white"]};
  border-top-left-radius: 15px;
  border-top-right-radius: 15px;
  flex: 1;
  padding: 25px 0 0;
`;
